
/**
 * Module dependencies.
 */

var youtube = require('youtube-feeds')

/**
 * A function to check if a specified YouTube video exists given an id.
 *
 * @param {string}       id The YouTube video id.
 * @param {mainCallback} cb The callback function that will handle the result.
 * @api public
 */

module.exports = function(id, cb) {
  youtube.video(id, function(err, result) {
    cb(!err && result && result.id === id)
  })
}

/**
 * Callback used by the main function.
 *
 * @callback mainCallback
 * @param {boolean} result True if the YouTube video exists.
 */
